# Yaml

> The Quiote\\Config\\Format\\Yaml namespace — 1 documented type.

Everything under `Quiote\Config\Format\Yaml`.

## Classes

| Class | Description |
|---|---|
| [`YamlPositionParser`](/api/config/format/yaml/yaml-position-parser/) | Best-effort key-path -> {file, line} index for a YAML config file, built by a block-style line scanner rather than a full YAML implementation. |
