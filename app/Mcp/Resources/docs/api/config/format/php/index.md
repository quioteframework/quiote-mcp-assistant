# Php

> The Quiote\\Config\\Format\\Php namespace — 1 documented type.

Everything under `Quiote\Config\Format\Php`.

## Classes

| Class | Description |
|---|---|
| [`PhpArrayPositionParser`](/api/config/format/php/php-array-position-parser/) | Best-effort key-path -> {file, line} index for a PHP-array config file's own `return [...]` literal. |
