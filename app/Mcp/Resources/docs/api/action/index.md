# Action

> The Quiote\\Action namespace — 3 documented types.

Everything under `Quiote\Action`.

## Classes

| Class | Description |
|---|---|
| [`Action`](/api/action/action/) | Base class for an application's actions: the unit that runs the business logic of one routed request and names the view that presents the result. |

## Traits

| Trait | Description |
|---|---|
| [`SlotCacheableTrait`](/api/action/slot-cacheable-trait/) | Optional trait for actions that want to customize slot caching TTL and tags. |

## Nested namespaces

| Namespace | Contents |
|---|---|
| [`Traits`](/api/action/traits/) | 1 type |
