# Twig

> The Quiote\\Renderer\\Twig namespace — 2 documented types.

Everything under `Quiote\Renderer\Twig`.

## Classes

| Class | Description |
|---|---|
| [`TemplateLayerLoader`](/api/renderer/twig/template-layer-loader/) | Twig loader that treats the template "name" Twig is given as a literal, already-resolved filesystem path. |
| [`TwigRenderer`](/api/renderer/twig/twig-renderer/) | Renders Twig (`.twig`) templates via twig/twig. |
