# CollectsCassetteRows

> Decodes every cassette a ListableCassetteStoreInterface holds into the small summary shape `cassette:list` and `cassette:prune` both filter and sort in PHP -- shared so the two commands never disagree about what a \"row\" is or how an unreadable cassette is reported.

Decodes every cassette a [`ListableCassetteStoreInterface`](/api/replay/store/listable-cassette-store-interface/) holds into the small summary shape `cassette:list` and `cassette:prune` both filter and sort in PHP -- shared so the two commands never disagree about what a "row" is or how an unreadable cassette is reported.

## Synopsis

`trait CollectsCassetteRows`

|  |  |
|---|---|
| Source | `Console/CollectsCassetteRows.php` |
