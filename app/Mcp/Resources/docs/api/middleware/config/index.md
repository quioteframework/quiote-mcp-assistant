# Config

> The Quiote\\Middleware\\Config namespace — 1 documented type.

Everything under `Quiote\Middleware\Config`.

## Classes

| Class | Description |
|---|---|
| [`MiddlewareConfigRegistry`](/api/middleware/config/middleware-config-registry/) | Process-global accumulator for `<use>` entries contributed by compiled `middleware.{xml,php,yaml,yml}` files (the app's own plus any module's `Config/middleware.*`), mirroring [`PluginManager`](/api/plugin/plugin-manager/)'s role for `plugins.*`. |
