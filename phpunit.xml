<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="tests/bootstrap.php"
         colors="true"
         cacheDirectory=".phpunit.cache">
    <testsuites>
        <testsuite name="unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="integration">
            <directory>tests/Integration</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
        <exclude>
            <directory>app/cache</directory>
            <directory>app/Mcp/Resources/docs</directory>
            <directory>app/pub</directory>
            <!-- Scaffold-generated placeholder Actions/Views (returns 'Success',
                 throws a framework exception) that exist only so this app's own
                 app/ has a real, introspectable website to self-target against,
                 not product logic worth measuring coverage on. -->
            <directory>app/Modules</directory>
        </exclude>
    </source>
    <coverage>
        <report>
            <html outputDirectory="build/coverage/html"/>
            <clover outputFile="build/coverage/clover.xml"/>
            <text outputFile="php://stdout" showUncoveredFiles="false"/>
        </report>
    </coverage>
</phpunit>
